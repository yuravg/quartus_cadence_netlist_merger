The directory contains files for testing 'qp_cnl_merger'

To use, you should run the program in this directory(the directory **test**) and check the
changes in the output files in the version control system.
